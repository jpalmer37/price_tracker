services:
  tracker:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_PATH: /data/price_tracker.db
    volumes:
      - tracker-data:/data

volumes:
  tracker-data:
